import React from 'react';
import styles from './header.module.css';

const Header = () => (
  <div className={styles.Header}>
    <div className="header">Bookshop MVP</div>
  </div>
);

export default Header;
